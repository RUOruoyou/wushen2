# wsmud2 开发规范

本文件适用于仓库根目录及全部子目录。所有开发者和自动化 Agent 在修改项目前，应先阅读本文件、`readme.md` 和 `PROJECT_DOC.md`。如果子目录后续增加更具体的 `AGENTS.md`，则子目录规则优先。

## 0. 沟通语言

- 与用户交流时全程使用中文，除非用户明确要求使用其他语言。


## 1. 项目定位与技术栈

wsmud2 是前后端和游戏服务同仓部署的 Node.js Web MUD：

- `web.js`：HTTP API 与 `www/` 静态资源服务。
- `main.js`：游戏 WebSocket 服务入口，加载 `os/` 引擎与 `world/` 内容。
- `api/`：CommonJS HTTP API。
- `data/`：SQLite 封装、数据库和服务器运行数据。
- `src/`：Vite + jQuery 前端源码，使用 ES Modules。
- `www/`：`npm run build` 生成的前端产物。
- `os/`：历史游戏核心引擎，依赖全局对象和动态脚本加载。
- `world/`：命令、地图、NPC、物品、技能、任务及引擎扩展。

运行环境以 Node.js `>= 24.10.0`、npm 和 `package-lock.json` 为准。

## 2. 修改原则

1. 先理解调用链再修改，优先做范围最小、可验证、可回滚的改动。
2. 遵循当前目录的既有实现方式，不为局部需求引入新的框架或大规模重构。
3. 新玩法和行为覆盖优先放入 `world/extends/`；新内容放入对应的 `world/*` 目录。
4. 尽量不直接修改 `os/`。只有无法通过 `world/extends/` 或内容脚本实现的底层需求，才允许修改引擎，并须扩大验证范围。
5. 不顺手清理无关旧代码，不批量改换行符、引号、缩进或文件编码。
6. 保持已有公开接口、命令名、对象路径、角色存档字段和消息结构向后兼容。必须破坏兼容性时，应同时提供迁移方案并更新文档。
7. 不通过复制粘贴制造重复逻辑；但只有确实减少复杂度或符合现有模式时才新增抽象。

## 3. 目录职责

| 路径 | 允许的主要改动 | 注意事项 |
| --- | --- | --- |
| `api/` | 登录、用户、服务器列表等 HTTP 接口 | 校验输入；保持 `{ code, result }` 等现有响应契约 |
| `data/db.js` | SQLite 底层连接和执行封装 | SQL 必须参数化；注意语句缓存与连接生命周期 |
| `data/sql.js` | 业务数据访问 | 数据库细节不要泄漏到前端或世界脚本 |
| `src/` | 前端页面、交互、样式和 WebSocket 客户端 | 保持 Vite、jQuery、`Page`/`Dialog`/`Process` 体系 |
| `www/` | 前端构建产物 | 不手工编辑；通过 `npm run build` 生成 |
| `world/cmd/` | 玩家命令、管理命令、弹窗命令 | 继承 `COMMAND`，保持权限和状态检查 |
| `world/map/`、`world/area/` | 房间与区域 | ID、出口、区域路径必须互相一致 |
| `world/npc/`、`world/obj/` | NPC、怪物、物品和装备 | 已发布对象路径和持久化字段不得随意重命名 |
| `world/skill/`、`world/family/` | 技能、绝招和门派 | 检查属性公式、消耗、冷却及战斗边界 |
| `world/task/` | 任务和事件 | 保持任务 ID、进度字段和奖励幂等性 |
| `world/extends/` | 对核心行为的增强和覆盖 | 首选扩展入口；确认启动加载顺序和热更新行为 |
| `os/` | 核心对象、网络、战斗、存档与加载器 | 高风险历史代码，非必要不改 |
| `deploy/`、`scripts/` | 部署和进程启动 | 不执行会改系统配置的脚本，除非任务明确要求 |

## 4. JavaScript 编写规范

### 4.1 通用规则

- 新代码使用 `const`；仅在确实重新赋值时使用 `let`，不要新增 `var`。
- 使用严格相等 `===` / `!==`，除非是在维护依赖宽松比较的旧协议行为。
- 函数和变量使用 `camelCase`，类使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`。
- 布尔变量使用 `is`、`has`、`can`、`should` 等清晰前缀。
- 单个函数只承担一个清晰职责；优先使用早返回减少深层嵌套。
- 异步操作使用 `async` / `await`，错误不能静默吞掉；在有恢复策略时记录必要上下文。
- 注释解释原因、协议限制或隐含约束，不重复代码表面含义。
- 用户可见文案使用中文并保持现有游戏语气；标识符、路径和协议字段保持英文。
- 修改旧文件时跟随该文件的缩进和引号风格。新文件默认 4 空格缩进、双引号、语句末尾加分号。
- 不因单次功能修改对整个旧文件运行格式化工具。

### 4.2 模块边界

- `web.js`、`api/`、`data/`、`os/` 使用 CommonJS：`require()` / `module.exports`。
- `src/` 使用 ES Modules：`import` / `export`。
- `world/` 脚本不是普通 Node.js 模块。它们由 `BASE.CREATE()` 动态执行，继续使用 `this.inherits(...)`、`this.set(...)`、`this.enter = ...` 等现有模式。
- 不在 `world/` 脚本中擅自改成 `module.exports`，也不要假设其拥有普通模块作用域。
- 新增全局对象前必须确认没有局部依赖注入或现有命名空间可用；避免继续扩大 `globalThis` 的使用范围。

## 5. Web/API 规范

- API 路由遵循 `/api/:className/:methodName`；新增 API 时同步注册到 `web.js` 的 `API_PATHS`。
- 所有外部输入都视为不可信：校验必填项、类型、长度、格式、允许值和权限。
- SQL 值必须通过 `?` 占位符传入，禁止拼接用户输入。动态列名或查询类型必须使用白名单。
- 认证、手机号、密码和管理接口不得在响应或日志中输出密码、哈希、Cookie、Session、密钥或完整个人信息。
- 保持现有成功/失败协议一致。修改响应字段前先搜索所有前端消费者。
- 对客户端返回稳定且可理解的错误信息；详细异常只记录在服务端。
- 涉及写操作时考虑重复请求、部分失败和幂等性，尤其是注册、角色、物品和奖励逻辑。

## 6. 前端规范

- 只编辑 `src/` 源码，构建产物由 Vite 输出到 `www/`。
- 沿用 `Page`、`Dialog`、`Process`、`SendCommand` 和 jQuery 事件委托，不在局部功能中引入另一套 UI 框架或状态管理库。
- 新增服务端消息类型时，同步检查 `src/process.js`、相关 `src/dialog/*` 和服务端发送方。
- 游戏 WebSocket 消息存在非严格 JSON 的历史协议；不要随意改为 `JSON.parse()`，除非服务端发送格式已完整迁移并经过兼容验证。
- DOM 事件优先绑定到稳定父节点，避免页面重复挂载后产生重复监听。
- UI 必须同时检查窄屏手机和项目现有的 768px 桌面布局；文字、按钮、弹窗和滚动区不得遮挡或溢出。
- CSS 优先复用现有类和主题变量，避免大量内联样式；修改公共选择器前搜索全部使用位置。
- 图标继续使用项目已有的 glyphicon/font 资源，除非任务包含完整的图标迁移。

## 7. 世界脚本规范

- 文件路径就是资源身份的一部分。命令、房间、NPC、物品、技能和任务路径使用小写英文及现有目录结构。
- 新命令必须设置明确的 `this.command`，并按业务需要配置 `allow_level`、`allow_busy`、`allow_fight`、`allow_state` 等限制。
- 访问玩家、环境或目标对象前做空值和状态检查；涉及战斗、背包和货币时同时检查数量边界。
- 奖励、扣费、物品转移和任务完成逻辑应避免重复执行造成刷取或丢失。
- 修改数值公式时明确加法、百分比、取整顺序，并验证 0、负数、极大值和缺失属性。
- 保持协议字段和存档字段可序列化；不要把连接、定时器、函数或循环引用写入持久化数据。
- 热更新路径不包含 `.js`，例如 `update cmd/action/look`。热更新后仍需考虑已有对象实例是否保留旧状态。
- 影响对象初始化、原型继承、全局数据或存档结构的修改，不能只依赖热更新验证，必须重启游戏服务验证完整加载。

## 8. 数据与安全红线

- 不读取、展示、提交或改写 `.env` 中的密钥；新增配置时只在文档中记录变量名和用途，不写真实值。
- `data/database.db`、`data/<serverId>/`、`logs/`、`mud.log`、`backups/` 和 `reports/` 属于运行数据或产物。除非任务明确要求，否则不要编辑、删除或提交其变化。
- 不在游戏服务运行时手工修改 `data/<serverId>/data.js`，服务保存可能覆盖改动。
- 数据库结构或存档格式变更前，先制定备份、兼容读取和回滚方案；不得直接执行不可恢复的数据清理。
- 禁止在源码中硬编码密码、Session 密钥、加密向量、生产域名凭据或管理员凭证。
- 不将未经验证的输入交给 `eval`、`new Function()`、`vm`、Shell 或动态资源路径。现有动态执行属于历史协议边界，不得扩大其输入来源。
- 管理命令必须保留并验证权限等级，不能仅依赖前端隐藏入口。

## 9. 依赖与生成文件

- 使用 npm，并保持 `package.json` 与 `package-lock.json` 同步。
- 新依赖必须有明确必要性，优先使用 Node.js 标准库和仓库已有依赖。
- 不直接编辑 `node_modules/`。
- 只有前端源码或构建配置变化时才重新生成 `www/`；构建前确认其中可能存在的用户改动。
- 不提交临时调试文件、进程 PID、日志、数据库副本或一次性报告。

## 10. 验证要求

项目当前没有有效的自动化测试，`npm test` 是会失败的占位脚本，不能把它当作验证结果。

按修改范围执行最小充分验证：

### 后端或启动代码

```bash
node --check web.js
node --check main.js
node --check api/user.js
```

只检查实际修改的 CommonJS 文件，并在需要时运行：

```bash
npm run web
npm run os
```

启动游戏服务可能连接并写入运行数据。除非验证确有需要，不要随意运行 `npm start`；启动后必须正常停止进程。

### 前端代码

```bash
npm run build
```

涉及交互或布局时，还要在浏览器中验证主要流程、控制台错误、移动端窄屏和 768px 布局。

### 世界内容

1. 对修改文件执行语法检查或通过服务加载确认无编译错误。
2. 使用管理员 `update <resource-path>` 验证可热更新逻辑。
3. 在游戏内覆盖成功、失败、权限不足、资源不足和重复操作等路径。
4. 涉及加载、继承或存档时重启游戏服务再验证一次。

### 数据库与协议

- 数据库修改至少验证新增、查询、更新、失败回滚和重启后读取。
- API 修改验证 HTTP 状态、响应结构、未登录和非法参数。
- WebSocket 修改验证连接、断线、重连、普通文本和对象消息。

无法执行某项验证时，交付说明中必须明确列出未验证项和原因。

## 11. 完成标准

提交或交付前确认：

- 改动只覆盖请求范围，没有误改运行数据或生成文件。
- 新逻辑符合所在层的模块体系和命名方式。
- 输入、权限、空值、边界值、重复执行和错误路径均已考虑。
- API、命令、资源路径、消息与存档兼容性已检查。
- 已运行与改动相匹配的验证，并记录结果。
- 行为、配置、部署方式或数据结构变化已同步更新 `readme.md`、`PROJECT_DOC.md` 或 `docs/`。
- 每次玩家可感知的功能开发、问题修复或配置调整完成后，在执行重启或生产部署之前，必须先拟定一份面向玩家的更新公告并交由用户确认。公告至少包含标题、摘要、主要更新内容和必要的玩家提示，只描述已经完成并验证的变化，不暴露代码路径、管理命令等实现细节，也不得把待开发或未验证内容写成已上线功能。未获得明确发布授权时，只拟稿，不得写入生产公告数据。
- 仅涉及管理页面或运营后台内部功能、布局和交互的改动，如果不改变玩家端界面、玩法规则、既有奖励、现有邮件内容或玩家存档，则无需拟定和发布面向玩家的更新公告，可在完成相应验证后直接部署。此例外不免除生产备份、部署验证和更新 `docs/开发日志.md` 的要求；一旦改动包含玩家可感知内容，仍按上一条执行公告确认流程。
- 每次功能开发、问题修复、配置调整或部署完成后，必须同步更新 `docs/开发日志.md`，记录完成日期、主要改动、验证结果和仍待处理的事项；未完成日志更新不得视为交付完成。

## 12. 常用命令

```bash
npm install          # 安装锁定依赖
npm start            # 同时启动 Web 和游戏服务
npm run web          # 仅启动 Web/API 服务
npm run os           # 仅启动游戏服务
npm run dev          # 启动 Vite 开发服务，默认端口 3333
npm run build        # 构建 src/ 到 www/
npm run preview      # 预览前端构建结果
```

执行部署、系统服务、Nginx、生产数据库或真实玩家数据相关操作前，必须确认目标环境、备份状态和任务授权。
